server:
  port: 9090

spring:
  application:
    name: dev-util
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://139.224.104.233:8826/develop-util?serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: root
    password: root
  redis:
    host: 127.0.0.1  # 主机地址
    port: 6379
    cache:
      redis:
        time-to-live: -1 #毫秒
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: c1ac94d8-353d-4d66-94e8-20215215dbd8
        username: nacos
        password: nacos
mybatis-plus:
  configuration:
    #默认不显示SQL日志
    #    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
logging:
  level:
    cn:
      panyucbale:
        springboot:
          mapper: debug

http:
  maxTotal: 100         #最大连接数
  defaultMaxPerRoute: 20  #并发数
  connectTimeout: 1000   #创建连接的最长时间
  connectionRequestTimeout: 500  #从连接池中获取到连接的最长时间
  socketTimeout: 10000 #数据传输的最长时间
  staleConnectionCheckEnabled: true  #提交请求前测试连接是否可用
  validateAfterInactivity: 3000000   #可用空闲连接过期时间,重用空闲连接时会先检查是否空闲时间超过这个时间，如果超过，释放socket重新建立

